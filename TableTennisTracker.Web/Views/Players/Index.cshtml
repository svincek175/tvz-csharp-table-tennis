@model IEnumerable<TableTennisTracker.Domain.Models.Player>

@{
    ViewData["Title"] = "Players";
    ViewData["Breadcrumbs"] = new List<(string, string)> { ("Home", "/"), ("Players", "/players/list") };
}

<style>
    .players-header {
        margin-bottom: 32px;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 16px;
    }

    .players-header h1 {
        font-size: 2.2rem;
        color: #e0e7ff;
        margin-bottom: 0;
        font-weight: 700;
    }

    .players-controls {
        display: flex;
        gap: 16px;
        align-items: center;
        flex-wrap: wrap;
        width: 100%;
    }

    .filter-select {
        min-width: 160px;
        padding: 10px 14px;
        background: rgba(139, 92, 246, 0.15);
        border: 1px solid rgba(139, 92, 246, 0.25);
        border-radius: 8px;
        color: #e0e7ff;
        font-size: 0.9rem;
    }

    .search-box {
        flex: 1;
        min-width: 0;
        width: 100%;
        padding: 10px 14px;
        background: rgba(139, 92, 246, 0.1);
        border: 1px solid rgba(139, 92, 246, 0.2);
        border-radius: 8px;
        color: #e0e7ff;
        font-size: 0.9rem;
    }

    .search-box::placeholder {
        color: #6b7280;
    }

    .btn-primary {
        background: rgba(139, 92, 246, 0.9);
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        font-size: 0.9rem;
        transition: background 0.3s;
    }

    .btn-primary:hover {
        background: rgba(139, 92, 246, 1);
    }

    .players-list {
        display: flex;
        flex-direction: column;
        gap: 12px;
    }

    .player-row {
        background: rgba(139, 92, 246, 0.08);
        border: 1px solid rgba(139, 92, 246, 0.15);
        border-radius: 8px;
        padding: 16px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: all 0.2s;
    }

    .player-row:hover {
        background: rgba(139, 92, 246, 0.12);
        border-color: rgba(139, 92, 246, 0.25);
    }

    .player-info {
        flex: 1;
    }

    .player-name {
        color: #e0e7ff;
        font-weight: 600;
        margin-bottom: 4px;
    }

    .player-meta {
        color: #9ca3af;
        font-size: 0.85rem;
    }

    .player-actions {
        display: flex;
        gap: 8px;
        margin-left: 16px;
    }

    .btn-small {
        padding: 6px 12px;
        font-size: 0.8rem;
        text-decoration: none;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        display: inline-block;
    }

    .btn-view {
        background: rgba(34, 197, 94, 0.2);
        color: #86efac;
        border: 1px solid rgba(34, 197, 94, 0.3);
    }

    .btn-edit {
        background: rgba(59, 130, 246, 0.2);
        color: #93c5fd;
        border: 1px solid rgba(59, 130, 246, 0.3);
    }

    .btn-delete {
        background: rgba(239, 68, 68, 0.2);
        color: #fca5a5;
        border: 1px solid rgba(239, 68, 68, 0.3);
    }

    .btn-small:hover {
        opacity: 0.8;
    }
</style>

<div class="players-header">
    <h1>🎾 Players</h1>
    <div class="players-controls">
        <select id="filterSelect" class="filter-select">
            <option value="all">All fields</option>
            <option value="name">Name</option>
            <option value="country">Country</option>
            <option value="status">Status</option>
        </select>
        <input type="text" id="searchInput" class="search-box" placeholder="Search players by name or country...">
        <a href="/players/create" class="btn-primary">+ Add New Player</a>
    </div>
</div>

<div id="resultsList" class="players-list">
    @await Html.PartialAsync("_PlayersList", Model)
</div>

<script>
    $(document).ready(function() {
        function refreshPlayers() {
            var query = $('#searchInput').val();
            var filter = $('#filterSelect').val();
            $.get('/players/search?query=' + encodeURIComponent(query) + '&filter=' + encodeURIComponent(filter), function(data) {
                $('#resultsList').html(data);
            });
        }

        $('#searchInput').on('keyup', refreshPlayers);
        $('#filterSelect').on('change', refreshPlayers);
    });
</script>

